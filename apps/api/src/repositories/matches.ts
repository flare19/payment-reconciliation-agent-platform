// ALL SQL for this table lives here and nowhere else (CLAUDE.md §4.1).
// Every decision-feeding query needs an explicit ORDER BY (ADR-032).
// snake_case in, camelCase out — this layer is the mapping boundary.
export {};
