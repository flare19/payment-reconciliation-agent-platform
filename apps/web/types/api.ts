/**
 * Wire types mirroring docs/api-contract.md §3.
 * Deliberately duplicated from apps/api rather than shared through a package:
 * the two apps deploy independently (ADR-058), and a shared package would add a
 * build step to both for a handful of interfaces.
 */
export {};
